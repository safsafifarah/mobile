import 'package:flutter/material.dart';

class OnHoldPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('On Hold Page'),
      ),
      body: Center(
        child: Text('On Hold'),
      ),
    );
  }
}
